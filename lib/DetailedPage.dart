import 'package:flutter/material.dart';

class DetailesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Detail Page - SSID"),
      ),
      body: const Center(
        child: Text("This is the detail page for SSID"),
      ),
    );
  }
}
